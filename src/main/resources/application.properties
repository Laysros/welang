server.port = 9999

#Basic Spring Boot Config for Oracle
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=dac
spring.datasource.password=dac
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

#hibernate config
spring.jpa.database-platform=org.hibernate.dialect.Oracle10gDialect

spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query

spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update
spring.jpa.generate-ddl=true
# Naming strategy


# Directory where will be saved uploaded files. Make sure that the application 
# have write permissions on such directory.
multipart.enabled=true
uploadedFiles = F:\DAC
multipart.max-file-size= 3MB
spring.resources.static-locations=classpath:/static/,file:/images/



spring.social.facebook.appId=1238926339473987
spring.social.facebook.appSecret=6b68fcdd7ea0ecec797765999d28e988

#
spring.thymeleaf.cache: false


#email
spring.mail.host = smtp.gmail.com
spring.mail.username = panda220v@gmail.com
spring.mail.password = xxx
spring.mail.port=587
spring.mail.properties.mail.smtp.starttls.enable = true
